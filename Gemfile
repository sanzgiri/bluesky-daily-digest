source "https://rubygems.org"

# GitHub Pages dependencies — pinned to match GH Pages
gem "github-pages", group: :jekyll_plugins
gem "jekyll-feed"
gem "jekyll-seo-tag"
gem "jekyll-paginate"
gem "jekyll-sitemap"

# For local preview only
group :jekyll_plugins do
  gem "webrick", "~> 1.8"
end
